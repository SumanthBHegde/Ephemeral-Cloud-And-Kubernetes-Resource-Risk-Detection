numpy>=1.26
pandas>=2.0
pyarrow>=14
PyYAML>=6.0
pytest>=8.0
# Stage 2 (detection): unsupervised Isolation Forest + ECOD ensemble
scikit-learn>=1.4
pyod>=2.0
scipy>=1.11
# Stage 3 (correlation): entity graph + connected-component incidents
networkx>=3.0
# Stage 5 (llm_triage): OpenAI structured-output triage agent (strict json_schema needs >=1.40)
openai>=1.40
python-dotenv>=1.0
# Stage 6 (report figures): matplotlib renders the static report PNGs from data/processed/*
matplotlib>=3.8
# Documentation: renders docs/report.md into the SG-styled docs/report.docx (docs/build_docx.py)
python-docx>=1.1
