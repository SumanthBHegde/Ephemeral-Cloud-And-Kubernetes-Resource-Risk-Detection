# Stage 5 (llm_triage) — copy to `.env` and fill in. `.env` is gitignored.
# Required only for live LLM triage (`python -m modules.llm_triage.build`).
# The `--no-llm` path and the test suite need no key.
OPENAI_API_KEY=
