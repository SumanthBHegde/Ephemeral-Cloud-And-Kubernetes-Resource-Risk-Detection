# Stage 5 — LLM Triage

Not yet built. A triage agent returning validated structured JSON (intent, confidence, MITRE techniques,
guardrails) per scored incident, with response caching and a templated fallback for the live demo.
