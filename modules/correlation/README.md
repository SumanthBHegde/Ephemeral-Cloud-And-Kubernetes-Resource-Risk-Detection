# Stage 3 — Graph Correlation

Not yet built. Builds an entity multigraph (principals, sessions, resources, namespaces, actions) over
detected candidates; incidents are connected components within an identity+namespace+time envelope.
